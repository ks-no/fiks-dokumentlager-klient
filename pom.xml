<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>no.ks.fiks.pom</groupId>
        <artifactId>fiks-ekstern-super-pom</artifactId>
        <version>0.0.3</version>
    </parent>

    <groupId>no.ks.fiks</groupId>
    <artifactId>dokumentlager-klient-parent</artifactId>
    <version>${revision}${changelist}</version>
    <packaging>pom</packaging>

    <name>Dokumentlager Java-klient</name>
    <description>Klient for opplasting, sletting og nedlasting av dokumenter fra Fiks Dokumentlager</description>
    <url>https://github.com/ks-no/fiks-dokumentlager-klient</url>

    <scm>
        <connection>scm:git:git@github.com:ks-no/fiks-dokumentlager-klient.git</connection>
        <developerConnection>scm:git:git@github.com:ks-no/fiks-dokumentlager-klient.git</developerConnection>
        <url>https://github.com/ks-no/fiks-dokumentlager-klient</url>
    </scm>

    <modules>
        <module>dokumentlager-klient</module>
        <module>dokumentlager-klient-spring-boot</module>
    </modules>

    <properties>
        <changelist>-SNAPSHOT</changelist>
        <revision>1.0.14</revision>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>1.8</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>

        <fiks-maskinporten-client.version>1.0.8</fiks-maskinporten-client.version>
        <fiks-kryptering.version>1.0.4-SNAPSHOT</fiks-kryptering.version>

        <spring-boot.version>2.1.2.RELEASE</spring-boot.version>

        <jackson.version>2.9.8</jackson.version>
        <commons-io.version>2.6</commons-io.version>
        <lombok.version>1.18.4</lombok.version>
        <validation-api.version>2.0.1.Final</validation-api.version>
        <jetty-client.version>9.4.13.v20181111</jetty-client.version>

        <maven-surefire-plugin.version>3.0.0-M3</maven-surefire-plugin.version>
        <junit-jupiter.version>5.4.0-M1</junit-jupiter.version>
        <hamcrest.version>1.3</hamcrest.version>
        <mockito-core.version>2.23.4</mockito-core.version>
        <logback-classic.version>1.2.3</logback-classic.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- FIKS avhenngigheter -->
            <dependency>
                <groupId>no.ks.fiks</groupId>
                <artifactId>dokumentlager-klient</artifactId>
                <version>${revision}${changelist}</version>
            </dependency>
            <dependency>
                <groupId>no.ks.fiks</groupId>
                <artifactId>dokumentlager-klient-spring-boot</artifactId>
                <version>${revision}${changelist}</version>
            </dependency>
            <dependency>
                <groupId>no.ks.fiks</groupId>
                <artifactId>maskinporten-client</artifactId>
                <version>${fiks-maskinporten-client.version}</version>
            </dependency>
            <dependency>
                <groupId>no.ks.fiks</groupId>
                <artifactId>maskinporten-spring-boot-client</artifactId>
                <version>${fiks-maskinporten-client.version}</version>
            </dependency>
            <dependency>
                <groupId>no.ks.fiks</groupId>
                <artifactId>kryptering</artifactId>
                <version>${fiks-kryptering.version}</version>
            </dependency>
            <!-- /FIKS avhenngigheter -->

            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-autoconfigure</artifactId>
                <version>${spring-boot.version}</version>
            </dependency>


            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-core</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons-io.version}</version>
            </dependency>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>
            <dependency>
                <groupId>javax.validation</groupId>
                <artifactId>validation-api</artifactId>
                <version>${validation-api.version}</version>
            </dependency>
            <dependency>
                <groupId>org.eclipse.jetty</groupId>
                <artifactId>jetty-client</artifactId>
                <version>${jetty-client.version}</version>
            </dependency>


            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-engine</artifactId>
                <version>${junit-jupiter.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-api</artifactId>
                <version>${junit-jupiter.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.hamcrest</groupId>
                <artifactId>hamcrest-core</artifactId>
                <version>${hamcrest.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito-core.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${logback-classic.version}</version>
                <scope>test</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven-surefire-plugin.version}</version>
            </plugin>
        </plugins>
    </build>
</project>